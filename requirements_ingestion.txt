# requirements_ingestion.txt
# ============================================================
# Install via: ./setup.sh  (handles venv automatically)
#
# System dependencies (macOS):
#   brew install poppler   # pdfinfo, pdftotext, pdffonts, pdftoppm
#   brew install tesseract # needed for scanned PDFs (e.g. Fitch)
# ============================================================

# PDF extraction
PyMuPDF==1.24.5
pdfplumber==0.11.0
pypdf==4.3.1

# Data handling
pandas==2.2.2
tabulate==0.9.0
openpyxl==3.1.2        # EBA Excel file parsing

# Validation
pydantic==2.7.1

# Utilities
loguru==0.7.2
tqdm==4.66.4
rich==13.7.1

# Downloading
requests==2.32.3
certifi==2024.2.2

# Training pair quality upgrade (one-time online step)
anthropic>=0.40.0

# Word document extraction (credit_reports/ folder)
python-docx==1.1.0
